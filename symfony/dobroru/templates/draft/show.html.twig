{% extends 'base.html.twig' %}

{% block title %}Draft{% endblock %}

{% block body %}
    <h1>Draft</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ draft.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ draft.name }}</td>
            </tr>
            <tr>
                <th>Clientname</th>
                <td>{{ draft.clientname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_draft_index') }}">back to list</a>

    <a href="{{ path('app_draft_edit', {'id': draft.id}) }}">edit</a>

    {{ include('draft/_delete_form.html.twig') }}
{% endblock %}
